name = "gnb-ai-cartoon"
main = "worker.js"
compatibility_date = "2024-11-01"

# Secrets are NOT stored here — set them with:
#   wrangler secret put REPLICATE_API_TOKEN
#   wrangler secret put REPLICATE_MODEL      (optional)
#   wrangler secret put ALLOW_ORIGIN         (optional, lock CORS to your site)
